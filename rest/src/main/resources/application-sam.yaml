spring:
  application:
    name: rent-sell-api
  liquibase:
    change-log: classpath:/db/master.xml
    enabled: true
  datasource:
    url: jdbc:mysql://localhost:3306/rent_sell
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  mail:
    host: smtp.gmail.com
    port: 587
    username: java2026java@gmail.com
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

system:
  upload:
    images:
      directory:
        path: C:\Users\samve\Real-estate-sales-and-rentals\app\upload-images\

server:
  port: 8081

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000
